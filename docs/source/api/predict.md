# Prediction

Predictions from a fitted `CrossedLMEResult`. The recommended interface is
`result.predict(...)` — this page documents the underlying function.

```{eval-rst}
.. autofunction:: interlace.predict.predict
```
